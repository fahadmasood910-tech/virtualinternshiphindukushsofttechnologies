import React from "react";

function Header() {
  return (
    <header
      style={{
        backgroundColor: "#007bff",
        color: "white",
        padding: "15px 0",
        fontSize: "24px",
      }}
    >
      My Profile App
    </header>
  );
}

export default Header;
